# dependencies
node_modules
.pnp
.pnp.*

# builds
.next
out
build
dist
*.tsbuildinfo
next-env.d.ts

# Deployable EB artifact (see apps/api/README.md) - ~70MB of vendored
# node_modules, rebuilt by `pnpm deploy` on every release.
.artifact

# env
.env
.env.*
!.env.example

# misc
.DS_Store
*.pem
coverage
.turbo
.vercel

# Transformer / Hugging Face caches (if any local ML tooling left behind)
.cache

# Classifier training venv / datasets (ONNX weights under apps/api/models are kept)
services/classifier/.venv
services/classifier/training/datasets/blobs
services/classifier/training/checkpoints



# local prod data exports (never commit)
.tmp-prod-dump/
